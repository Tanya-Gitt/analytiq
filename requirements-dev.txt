# Development + test dependencies
-r requirements.txt

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27         # already in main, explicit for test clarity

# Linting
ruff>=0.4.0
mypy>=1.10.0
types-cachetools>=5.3.0
