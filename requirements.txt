# Unified Analytics Platform — runtime dependencies
# pandas, sqlalchemy, pymysql removed (replaced by asyncpg + PostgreSQL)

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # required by FastAPI for File/UploadFile (file upload endpoints)

# Database
asyncpg>=0.29.0

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0

# HTTP client (for sheets_csv connector: fetch public CSV URL)
httpx>=0.27.0

# Pydantic (bundled with FastAPI but pinned for stability)
pydantic[email]>=2.7.0

# Scheduler
apscheduler>=3.10.0

# Config
python-dotenv>=1.0.0

# Caching (dashboard TTL cache)
cachetools>=5.3.0
